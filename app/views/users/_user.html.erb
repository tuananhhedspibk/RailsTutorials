<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user_path(user) %>
  <% if current_user.admin? && !user.current_user?(current_user) %>
    | <%= link_to t("delete"), user_path(user), method: :delete,
      data: {confirm: t("you_sure")} %>
  <% end %>
</li>
